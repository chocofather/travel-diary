spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mydb?serverTimezone=UTC&charEncoding=UTF-8
    username: localmaster
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 10MB
  web:
    resources:
      static-locations: classpath:/static/
  mail:
    host: smtp.gmail.com
    port: 587
    username: rlaalswns0147@gmail.com
    password: zdcn voyw xlww gkeb
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
        debug: true

custom:
  server-url: http://localhost:8080
  upload-path: /Users/minjun/travlediary-uploads/ # ✅ 이 줄 추가

server:
  servlet:
    session:
      tracking-modes: cookie

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.example.travlediary.model
  configuration:
    map-underscore-to-camel-case: true

security:
  user:
    name: admin
    password: admin

logging:
  level:
    org.springframework.security: DEBUG
    org.springframework.web.servlet.mvc.method.annotation.RequestMappingHandlerMapping: INFO

