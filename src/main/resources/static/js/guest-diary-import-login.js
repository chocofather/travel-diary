/*
 * 로그인 화면에서 "체험 여행일기 저장" 으로 돌아갈 자리를 다시 붙인다.
 *
 * 보통은 서버가 기억해 둔다. 로그인해야 열리는 자리로 갔다가 로그인 화면으로 밀려나면
 * 서버가 원래 가려던 곳을 들고 있다가 인증 뒤에 그리로 데려다준다.
 *
 * 다만 회원가입 → 이메일 인증 → 로그인처럼 중간에 세션이 끊기면 그 기억이 사라진다.
 * 그때만 이 브라우저에 남은 쪽지를 보고 다시 채운다.
 * 채우는 값은 쪽지에 적힌 것이 아니라 고정 내부 경로라, 바깥 주소가 들어올 자리가 없다.
 */
document.addEventListener('DOMContentLoaded', () => {
    window.GuestDiaryImportIntent?.applyToLoginForm();
});
