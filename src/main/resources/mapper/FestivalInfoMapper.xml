<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.travlediary.repository.travelinfo.FestivalInfoMapper">

    <resultMap id="FestivalInfoResultMap" type="com.example.travlediary.model.FestivalInfo">
        <id property="infoId" column="info_id"/>
        <result property="eventPlace" column="event_place"/>
        <result property="address" column="address"/>
        <result property="playTime" column="play_time"/>
        <result property="useTime" column="use_time"/>
        <result property="sponsor1" column="sponsor1"/>
        <result property="sponsor1Tel" column="sponsor1_tel"/>
        <result property="sponsor2" column="sponsor2"/>
        <result property="sponsor2Tel" column="sponsor2_tel"/>
        <result property="contactTel" column="contact_tel"/>
        <result property="homepageUrl" column="homepage_url"/>
        <result property="sourceType" column="source_type"/>
        <result property="externalContentId" column="external_content_id"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="findByInfoId" parameterType="long" resultMap="FestivalInfoResultMap">
        SELECT info_id,
               event_place,
               address,
               play_time,
               use_time,
               sponsor1,
               sponsor1_tel,
               sponsor2,
               sponsor2_tel,
               contact_tel,
               homepage_url,
               source_type,
               external_content_id,
               created_at,
               updated_at
        FROM festival_info
        WHERE info_id = #{infoId}
    </select>

    <select id="countBySourceTypeAndExternalContentId" resultType="int">
        SELECT COUNT(*)
        FROM festival_info
        WHERE source_type = #{sourceType}
          AND external_content_id = #{externalContentId}
    </select>

    <insert id="insert" parameterType="com.example.travlediary.model.FestivalInfo">
        INSERT INTO festival_info (
            info_id,
            event_place,
            address,
            play_time,
            use_time,
            sponsor1,
            sponsor1_tel,
            sponsor2,
            sponsor2_tel,
            contact_tel,
            homepage_url,
            source_type,
            external_content_id
        )
        VALUES (
            #{infoId},
            #{eventPlace},
            #{address},
            #{playTime},
            #{useTime},
            #{sponsor1},
            #{sponsor1Tel},
            #{sponsor2},
            #{sponsor2Tel},
            #{contactTel},
            #{homepageUrl},
            COALESCE(#{sourceType}, 'ADMIN'),
            #{externalContentId}
        )
    </insert>

    <update id="update" parameterType="com.example.travlediary.model.FestivalInfo">
        UPDATE festival_info
        SET event_place = #{eventPlace},
            address = #{address},
            play_time = #{playTime},
            use_time = #{useTime},
            sponsor1 = #{sponsor1},
            sponsor1_tel = #{sponsor1Tel},
            sponsor2 = #{sponsor2},
            sponsor2_tel = #{sponsor2Tel},
            contact_tel = #{contactTel},
            homepage_url = #{homepageUrl},
            source_type = COALESCE(#{sourceType}, 'ADMIN'),
            external_content_id = #{externalContentId}
        WHERE info_id = #{infoId}
    </update>

    <delete id="deleteByInfoId" parameterType="long">
        DELETE FROM festival_info
        WHERE info_id = #{infoId}
    </delete>

</mapper>
