<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.travlediary.repository.info.ShopInfoMapper">

    <insert id="insert" parameterType="com.example.travlediary.model.ShopInfo">
        INSERT INTO shop_info
        (destination_id, closed_days, opening_hours, main_products, parking_available, contact_number, homepage_url, guide)
        VALUES
            (#{destinationId}, #{closedDays}, #{openingHours}, #{mainProducts}, #{parkingAvailable}, #{contactNumber}, #{homepageUrl}, #{guide})
    </insert>

    <select id="findByDestinationId" resultType="com.example.travlediary.model.ShopInfo">
        SELECT * FROM shop_info WHERE destination_id = #{destinationId}
    </select>

    <!-- 언어별 자유 텍스트. 화면에 쓸 값은 서비스가 고른다. (필드마다 따로 대체) -->
    <select id="findTranslationsByDestinationId"
            resultType="com.example.travlediary.model.ShopInfoTranslation">
        SELECT id,
               destination_id,
               language_code,
               closed_days,
               opening_hours,
               main_products,
               guide
        FROM shop_info_translations
        WHERE destination_id = #{destinationId}
        ORDER BY language_code ASC, id ASC
    </select>

    <!-- 관리자 저장. 언어 한 줄이 단위이며 UNIQUE(destination_id, language_code) 를 따른다. -->
    <insert id="insertTranslation"
            parameterType="com.example.travlediary.model.ShopInfoTranslation">
        INSERT INTO shop_info_translations
            (destination_id, language_code, closed_days, opening_hours, main_products, guide)
        VALUES
            (#{destinationId}, #{languageCode}, #{closedDays}, #{openingHours}, #{mainProducts}, #{guide})
    </insert>

    <update id="updateTranslation"
            parameterType="com.example.travlediary.model.ShopInfoTranslation">
        UPDATE shop_info_translations
        SET closed_days = #{closedDays},
            opening_hours = #{openingHours},
            main_products = #{mainProducts},
            guide = #{guide}
        WHERE destination_id = #{destinationId}
          AND language_code = #{languageCode}
    </update>

    <delete id="deleteTranslation">
        DELETE FROM shop_info_translations
        WHERE destination_id = #{destinationId}
          AND language_code = #{languageCode}
    </delete>

    <update id="update" parameterType="com.example.travlediary.model.ShopInfo">
        UPDATE shop_info
        SET
            closed_days = #{closedDays},
            opening_hours = #{openingHours},
            main_products = #{mainProducts},
            parking_available = #{parkingAvailable},
            contact_number = #{contactNumber},
            homepage_url = #{homepageUrl},
            guide = #{guide}
        WHERE destination_id = #{destinationId}
    </update>


</mapper>
