<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.travlediary.repository.info.AccommodationInfoMapper">

    <insert id="insert" parameterType="com.example.travlediary.model.AccommodationInfo">
        INSERT INTO accommodation_info
        (destination_id, checkin_time, checkout_time, room_count, room_type, star_rating, breakfast_included, parking_available, pet_allowed, contact_number, homepage_url, etc)
        VALUES
            (#{destinationId}, #{checkinTime}, #{checkoutTime}, #{roomCount}, #{roomType}, #{starRating}, #{breakfastIncluded}, #{parkingAvailable}, #{petAllowed}, #{contactNumber}, #{homepageUrl}, #{etc})
    </insert>

    <select id="findByDestinationId" resultType="com.example.travlediary.model.AccommodationInfo">
        SELECT * FROM accommodation_info WHERE destination_id = #{destinationId}
    </select>

    <!-- 언어별 자유 텍스트. 화면에 쓸 값은 서비스가 고른다. (필드마다 따로 대체) -->
    <select id="findTranslationsByDestinationId"
            resultType="com.example.travlediary.model.AccommodationInfoTranslation">
        SELECT id,
               destination_id,
               language_code,
               room_type,
               etc
        FROM accommodation_info_translations
        WHERE destination_id = #{destinationId}
        ORDER BY language_code ASC, id ASC
    </select>

    <!-- 관리자 저장. 언어 한 줄이 단위이며 UNIQUE(destination_id, language_code) 를 따른다. -->
    <insert id="insertTranslation"
            parameterType="com.example.travlediary.model.AccommodationInfoTranslation">
        INSERT INTO accommodation_info_translations
            (destination_id, language_code, room_type, etc)
        VALUES
            (#{destinationId}, #{languageCode}, #{roomType}, #{etc})
    </insert>

    <update id="updateTranslation"
            parameterType="com.example.travlediary.model.AccommodationInfoTranslation">
        UPDATE accommodation_info_translations
        SET room_type = #{roomType},
            etc = #{etc}
        WHERE destination_id = #{destinationId}
          AND language_code = #{languageCode}
    </update>

    <delete id="deleteTranslation">
        DELETE FROM accommodation_info_translations
        WHERE destination_id = #{destinationId}
          AND language_code = #{languageCode}
    </delete>

    <update id="update" parameterType="com.example.travlediary.model.AccommodationInfo">
        UPDATE accommodation_info
        SET
            checkin_time = #{checkinTime},
            checkout_time = #{checkoutTime},
            room_count = #{roomCount},
            room_type = #{roomType},
            star_rating = #{starRating},
            breakfast_included = #{breakfastIncluded},
            parking_available = #{parkingAvailable},
            pet_allowed = #{petAllowed},
            contact_number = #{contactNumber},
            homepage_url = #{homepageUrl},
            etc = #{etc}
        WHERE destination_id = #{destinationId}
    </update>


</mapper>
