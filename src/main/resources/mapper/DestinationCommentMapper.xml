<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.travlediary.repository.comment.DestinationCommentMapper">

    <resultMap id="DestinationCommentMap" type="com.example.travlediary.model.DestinationComment">
        <id property="id" column="id"/>
        <result property="parentCommentId" column="parent_comment_id"/>
        <result property="content" column="content"/>
        <result property="imageUrl" column="image_url"/>
        <result property="likes" column="likes"/>
        <result property="deleted" column="deleted"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
        <result property="deletedAt" column="deleted_at"/>
        <result property="userId" column="user_id"/>
        <result property="destinationId" column="destination_id"/>
    </resultMap>


    <resultMap id="commentWithWriterMap" type="com.example.travlediary.model.DestinationComment">
        <id     property="id"         column="c_id"/>
        <result property="content"    column="content"/>
        <result property="imageUrl"   column="image_url"/>
        <result property="createdAt"  column="created_at"/>
        <result property="updatedAt"  column="updated_at"/>
        <result property="likes"      column="likes"/>
        <result property="parentCommentId" column="parent_comment_id"/>
        <result property="userId"     column="user_id"/>
        <result property="deleted"    column="deleted"/>
        <result property="moderated"  column="moderated"/>

        <!-- nested user -->
        <association property="writer" javaType="com.example.travlediary.model.User">
            <id     property="id"            column="u_id"/>
            <result property="nickname"      column="nickname"/>
            <result property="profileImage"  column="profile_image"/>
        </association>
    </resultMap>


    <!-- 관리자 조치로 숨겨진 댓글 판별. 사용자 직접 삭제분은 포함하지 않는다. -->
    <sql id="ModeratedExists">
        EXISTS (
            SELECT 1
            FROM content_moderations cm
            WHERE cm.target_type = 'DESTINATION_COMMENT'
              AND cm.target_id = c.id
              AND cm.status = 'ACTIVE'
        )
    </sql>

    <sql id="ModeratedFlag">
        <include refid="ModeratedExists"/> AS moderated,
    </sql>

    <!-- 댓글 등록 -->
    <insert id="insert" parameterType="DestinationComment" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO destination_comments (
            destination_id, user_id, content, image_url, likes, deleted,
            created_at, updated_at, parent_comment_id
        )
        VALUES (
                   #{destinationId}, #{userId}, #{content}, #{imageUrl}, #{likes}, #{deleted},
                   #{createdAt}, #{updatedAt}, #{parentCommentId}
               )
    </insert>

    <!-- 특정 여행지의 댓글 목록 조회 (삭제되지 않은 것만) -->
    <select id="findByDestinationId" resultType="com.example.travlediary.model.DestinationComment">
        SELECT *
        FROM destination_comments
        WHERE destination_id = #{destinationId}
          AND deleted = false
        ORDER BY created_at ASC
    </select>

    <!-- 댓글 1개 조회 -->
    <select id="findById" resultType="com.example.travlediary.model.DestinationComment">
        SELECT * FROM destination_comments WHERE id = #{id}
    </select>

    <!-- 소프트 삭제 처리 (deleted = true, 삭제일 저장) -->
    <update id="updateDeleted">
        UPDATE destination_comments
        SET deleted = true,
            deleted_at = #{deletedAt}
        WHERE id = #{id}
    </update>

    <!-- 댓글 수정 쿼리: 내용 + 수정시간 갱신 -->
    <update id="updateContent" parameterType="com.example.travlediary.model.DestinationComment">
        UPDATE destination_comments
        SET content = #{content},
            updated_at = #{updatedAt}
        WHERE id = #{id}
    </update>

    <!-- 댓글 수 조회 (삭제되지 않은 것만) -->
    <select id="countByDestinationId" resultType="int">
        SELECT COUNT(*)
        FROM destination_comments
        WHERE destination_id = #{destinationId}
          AND deleted = 0
    </select>

    <!-- 페이징 목록과 같은 노출 조건을 써야 마지막 페이지 판정이 어긋나지 않는다. -->
    <select id="countRootComments" resultType="int">
        SELECT COUNT(*)
        FROM destination_comments c
        WHERE c.destination_id = #{destinationId}
          AND c.parent_comment_id IS NULL
          AND (c.deleted = 0 OR <include refid="ModeratedExists"/>)
    </select>

    <select id="findActiveRootIdForLocation" resultType="long">
        SELECT root.id
        FROM destination_comments target
        JOIN destination_comments root
          ON root.id = COALESCE(target.parent_comment_id, target.id)
         AND root.destination_id = target.destination_id
         AND root.parent_comment_id IS NULL
         AND root.deleted = 0
        JOIN destinations d ON d.id = target.destination_id
        WHERE target.id = #{commentId}
          AND target.destination_id = #{destinationId}
          AND target.deleted = 0
    </select>

    <select id="countRootCommentsBefore" resultType="int">
        SELECT COUNT(*)
        FROM destination_comments preceding
        JOIN destination_comments target_root
          ON target_root.id = #{rootId}
         AND target_root.destination_id = #{destinationId}
         AND target_root.parent_comment_id IS NULL
         AND target_root.deleted = 0
        WHERE preceding.destination_id = #{destinationId}
          AND preceding.parent_comment_id IS NULL
          AND preceding.deleted = 0
          AND (
              (target_root.created_at IS NULL AND preceding.created_at IS NOT NULL)
              OR preceding.created_at &gt; target_root.created_at
              OR (
                  (preceding.created_at = target_root.created_at
                      OR (preceding.created_at IS NULL AND target_root.created_at IS NULL))
                  AND preceding.id &gt; target_root.id
              )
          )
    </select>

    <!-- 댓글 이미지 추출 -->
    <select id="selectCommentsWithImages" resultMap="DestinationCommentMap">
        SELECT *
        FROM destination_comments
        WHERE destination_id = #{destinationId}
          AND image_url IS NOT NULL
          AND deleted = 0
        ORDER BY created_at DESC
            LIMIT 12
    </select>

    <select id="findByDestinationIdWithWriter" resultMap="commentWithWriterMap">
    SELECT
        c.id as c_id,
        CASE WHEN c.deleted = 1 THEN NULL ELSE c.content END AS content,
        CASE WHEN c.deleted = 1 THEN NULL ELSE c.image_url END AS image_url,
        c.created_at,
        c.updated_at,
        c.likes,
        c.parent_comment_id,
        c.user_id,
        c.deleted,
        <include refid="ModeratedFlag"/>

        u.id as u_id,
        CASE WHEN c.deleted = 1 THEN NULL ELSE u.nickname END AS nickname,
        CASE WHEN c.deleted = 1 THEN NULL ELSE u.profile_image END AS profile_image

    FROM destination_comments c
             JOIN users u ON c.user_id = u.id
    WHERE c.destination_id = #{destinationId}
      AND (c.deleted = 0 OR <include refid="ModeratedExists"/>)
    ORDER BY c.created_at ASC
</select>

    <!-- 최신순 -->
    <select id="findByDestinationIdOrderByCreatedAtDesc" resultMap="commentWithWriterMap">
        SELECT
            c.id as c_id,
            CASE WHEN c.deleted = 1 THEN NULL ELSE c.content END AS content,
            CASE WHEN c.deleted = 1 THEN NULL ELSE c.image_url END AS image_url,
            c.created_at,
            c.updated_at,
            c.likes,
            c.parent_comment_id,
            c.user_id,
            c.deleted,
            <include refid="ModeratedFlag"/>

            u.id as u_id,
            CASE WHEN c.deleted = 1 THEN NULL ELSE u.nickname END AS nickname,
            CASE WHEN c.deleted = 1 THEN NULL ELSE u.profile_image END AS profile_image

        FROM destination_comments c
                 JOIN users u ON c.user_id = u.id
        WHERE c.destination_id = #{destinationId}
          AND (c.deleted = 0 OR <include refid="ModeratedExists"/>)
        ORDER BY c.created_at DESC
    </select>

    <!-- 좋아요순 -->
    <select id="findByDestinationIdOrderByLikesDesc" resultMap="commentWithWriterMap">
        SELECT
            c.id as c_id,
            CASE WHEN c.deleted = 1 THEN NULL ELSE c.content END AS content,
            CASE WHEN c.deleted = 1 THEN NULL ELSE c.image_url END AS image_url,
            c.created_at,
            c.updated_at,
            c.likes,
            c.parent_comment_id,
            c.user_id,
            c.deleted,
            <include refid="ModeratedFlag"/>

            u.id as u_id,
            CASE WHEN c.deleted = 1 THEN NULL ELSE u.nickname END AS nickname,
            CASE WHEN c.deleted = 1 THEN NULL ELSE u.profile_image END AS profile_image

        FROM destination_comments c
                 JOIN users u ON c.user_id = u.id
        WHERE c.destination_id = #{destinationId}
          AND (c.deleted = 0 OR <include refid="ModeratedExists"/>)
        ORDER BY c.likes DESC, c.created_at ASC
    </select>

    <!-- 좋아요 여부 -->
    <select id="existsLikeByUserAndComment" resultType="boolean">
        SELECT EXISTS(
            SELECT 1 FROM comment_likes
            WHERE user_id = #{userId} AND comment_id = #{commentId}
        )
    </select>

    <select id="findImagePathById" resultType="String">
        SELECT image_path
        FROM destination_comments
        WHERE id = #{id}
    </select>

    <update id="updateImagePath">
        UPDATE destination_comments
        SET image_path = #{imagePath},
            updated_at = NOW()
        WHERE id = #{id}
    </update>

   <!-- <select id="findPagedComments" resultMap="commentWithWriterMap">
        SELECT
        c.id as c_id,
        c.content,
        c.image_url,
        c.created_at,
        c.updated_at,
        c.likes,
        c.parent_comment_id,
        c.user_id,

        u.id as u_id,
        u.nickname,
        u.profile_image

        FROM destination_comments c
        JOIN users u ON c.user_id = u.id
        WHERE c.destination_id = #{destinationId}
        AND c.deleted = 0
        <choose>
            <when test="sort == 'recent'">
                ORDER BY c.created_at DESC
            </when>
            <when test="sort == 'likes'">
                ORDER BY c.likes DESC, c.created_at ASC
            </when>
            <otherwise>
                ORDER BY c.created_at ASC
            </otherwise>
        </choose>
        LIMIT #{limit} OFFSET #{offset}
    </select>-->

    <select id="countComments" resultType="int">
        SELECT COUNT(*)
        FROM destination_comments
        WHERE destination_id = #{destinationId}
          AND deleted = 0
    </select>

    <!-- 오래된 순 -->
    <select id="findPagedByCreatedAtAsc" resultMap="commentWithWriterMap">
        SELECT
            c.id as c_id, c.content, c.image_url, c.created_at, c.updated_at, c.likes,
            c.parent_comment_id, c.user_id,
            u.id as u_id, u.nickname, u.profile_image
        FROM destination_comments c
                 JOIN users u ON c.user_id = u.id
        WHERE c.destination_id = #{destinationId}
          AND c.deleted = 0
        ORDER BY c.created_at ASC
            LIMIT #{limit} OFFSET #{offset}
    </select>

    <!-- 최신순 -->
    <select id="findPagedByCreatedAtDesc" resultMap="commentWithWriterMap">
        SELECT
            c.id as c_id, c.content, c.image_url, c.created_at, c.updated_at, c.likes,
            c.parent_comment_id, c.user_id,
            u.id as u_id, u.nickname, u.profile_image
        FROM destination_comments c
                 JOIN users u ON c.user_id = u.id
        WHERE c.destination_id = #{destinationId}
          AND c.deleted = 0
        ORDER BY c.created_at DESC
            LIMIT #{limit} OFFSET #{offset}
    </select>

    <!-- 좋아요순 -->
    <select id="findPagedByLikesDesc" resultMap="commentWithWriterMap">
        SELECT
            c.id as c_id, c.content, c.image_url, c.created_at, c.updated_at, c.likes,
            c.parent_comment_id, c.user_id,
            u.id as u_id, u.nickname, u.profile_image
        FROM destination_comments c
                 JOIN users u ON c.user_id = u.id
        WHERE c.destination_id = #{destinationId}
          AND c.deleted = 0
        ORDER BY c.likes DESC, c.created_at ASC
            LIMIT #{limit} OFFSET #{offset}
    </select>

    <!-- 1. 부모 댓글 페이징 조회 -->
    <select id="findPagedParentComments" resultMap="commentWithWriterMap">
        SELECT
        c.id as c_id,
        CASE WHEN c.deleted = 1 THEN NULL ELSE c.content END AS content,
        CASE WHEN c.deleted = 1 THEN NULL ELSE c.image_url END AS image_url,
        c.created_at, c.updated_at, c.likes,
        c.parent_comment_id, c.user_id,
        c.deleted,
        <include refid="ModeratedFlag"/>
        u.id as u_id,
        CASE WHEN c.deleted = 1 THEN NULL ELSE u.nickname END AS nickname,
        CASE WHEN c.deleted = 1 THEN NULL ELSE u.profile_image END AS profile_image
        FROM destination_comments c
        JOIN users u ON c.user_id = u.id
        WHERE c.destination_id = #{destinationId}
        AND c.parent_comment_id IS NULL
        AND (c.deleted = 0 OR <include refid="ModeratedExists"/>)
        <choose>
            <when test="sort == 'oldest'">
                ORDER BY c.created_at ASC, c.id ASC
            </when>
            <when test="sort == 'likes'">
                ORDER BY c.likes DESC, c.created_at DESC, c.id DESC
            </when>
            <otherwise>
                ORDER BY c.created_at DESC, c.id DESC
            </otherwise>
        </choose>
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <!-- 2. 부모댓글 id 목록에 해당하는 자식(대댓글) 전체 조회 -->
    <select id="findRepliesForParents" resultMap="commentWithWriterMap">
        SELECT
        c.id as c_id,
        CASE WHEN c.deleted = 1 THEN NULL ELSE c.content END AS content,
        CASE WHEN c.deleted = 1 THEN NULL ELSE c.image_url END AS image_url,
        c.created_at, c.updated_at, c.likes,
        c.parent_comment_id, c.user_id,
        c.deleted,
        <include refid="ModeratedFlag"/>
        u.id as u_id,
        CASE WHEN c.deleted = 1 THEN NULL ELSE u.nickname END AS nickname,
        CASE WHEN c.deleted = 1 THEN NULL ELSE u.profile_image END AS profile_image
        FROM destination_comments c
        JOIN users u ON c.user_id = u.id
        WHERE c.parent_comment_id IN
        <foreach item="id" collection="parentIds" open="(" separator="," close=")">
            #{id}
        </foreach>
        AND c.destination_id = #{destinationId}
        AND (c.deleted = 0 OR <include refid="ModeratedExists"/>)
        ORDER BY c.created_at ASC, c.id ASC
    </select>

    <!-- 여러 여행지 댓글 수 카운트-->
    <select id="countByDestinationIds" resultType="map">
        SELECT destination_id, COUNT(*) AS cnt
        FROM destination_comments
        WHERE destination_id IN
        <foreach item="id" collection="destinationIds" open="(" separator="," close=")">
            #{id}
        </foreach>
        AND deleted = 0
        GROUP BY destination_id
    </select>


</mapper>
