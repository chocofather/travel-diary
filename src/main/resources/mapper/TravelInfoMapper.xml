<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.travlediary.repository.travelinfo.TravelInfoMapper">

    <resultMap id="TravelInfoResultMap" type="com.example.travlediary.model.TravelInfo">
        <id property="id" column="id"/>
        <result property="title" column="title"/>
        <result property="content" column="content"/>
        <result property="scope" column="scope"/>
        <result property="contentType" column="content_type"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
        <result property="categoryId" column="category_id"/>
        <result property="views" column="views"/>
        <result property="userId" column="user_id"/>
    </resultMap>

    <resultMap id="AdminListResultMap" type="com.example.travlediary.dto.AdminTravelInfoListItemDto">
        <id property="id" column="id"/>
        <result property="title" column="title"/>
        <result property="scope" column="scope"/>
        <result property="contentType" column="content_type"/>
        <result property="categoryId" column="category_id"/>
        <result property="categoryName" column="category_name"/>
        <result property="views" column="views"/>
        <result property="createdAt" column="created_at"/>
        <result property="startDate" column="start_date"/>
        <result property="endDate" column="end_date"/>
    </resultMap>

    <resultMap id="PublicListResultMap" type="com.example.travlediary.dto.TravelInfoListItemDto">
        <id property="id" column="id"/>
        <result property="title" column="title"/>
        <result property="scope" column="scope"/>
        <result property="contentType" column="content_type"/>
        <result property="categoryId" column="category_id"/>
        <result property="categoryName" column="category_name"/>
        <result property="thumbnailUrl" column="thumbnail_url"/>
        <result property="views" column="views"/>
        <result property="createdAt" column="created_at"/>
        <result property="startDate" column="start_date"/>
        <result property="endDate" column="end_date"/>
    </resultMap>

    <resultMap id="PublicDetailResultMap" type="com.example.travlediary.dto.TravelInfoDetailDto">
        <id property="id" column="id"/>
        <result property="title" column="title"/>
        <result property="scope" column="scope"/>
        <result property="contentType" column="content_type"/>
        <result property="categoryId" column="category_id"/>
        <result property="categoryName" column="category_name"/>
        <result property="content" column="content"/>
        <result property="views" column="views"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <resultMap id="InfoPeriodResultMap" type="com.example.travlediary.model.InfoPeriod">
        <id property="id" column="id"/>
        <result property="startDate" column="start_date"/>
        <result property="endDate" column="end_date"/>
        <result property="infoId" column="info_id"/>
    </resultMap>

    <resultMap id="TravelInfoTranslationResultMap"
               type="com.example.travlediary.model.TravelInfoTranslation">
        <id property="id" column="id"/>
        <result property="travelInfoId" column="travel_info_id"/>
        <result property="languageCode" column="language_code"/>
        <result property="title" column="title"/>
        <result property="content" column="content"/>
    </resultMap>

    <resultMap id="InfoImageResultMap" type="com.example.travlediary.model.InfoImage">
        <id property="id" column="id"/>
        <result property="imageUrl" column="image_url"/>
        <result property="sourceType" column="source_type"/>
        <result property="sourceName" column="source_name"/>
        <result property="externalContentId" column="external_content_id"/>
        <result property="sourceTitle" column="source_title"/>
        <result property="licenseType" column="license_type"/>
        <result property="sourceImageUrl" column="source_image_url"/>
        <result property="licenseCheckedAt" column="license_checked_at"/>
        <result property="isMain" column="is_main"/>
        <result property="isThumbnail" column="is_thumbnail"/>
        <result property="orderIndex" column="order_index"/>
        <result property="createdAt" column="created_at"/>
        <result property="infoId" column="info_id"/>
    </resultMap>

    <select id="findAdminList" resultMap="AdminListResultMap">
        SELECT ti.id,
               ti.title,
               ti.scope,
               ti.content_type,
               ti.category_id,
               ic.name AS category_name,
               ti.views,
               ti.created_at,
               festival_period.start_date,
               festival_period.end_date
        FROM travel_info ti
        JOIN info_categories ic ON ic.id = ti.category_id
        LEFT JOIN info_periods festival_period
          ON festival_period.id = (
              SELECT period_candidate.id
              FROM info_periods period_candidate
              WHERE period_candidate.info_id = ti.id
                AND ti.content_type = 'FESTIVAL'
              ORDER BY period_candidate.start_date ASC, period_candidate.end_date ASC, period_candidate.id ASC
              LIMIT 1
          )
         AND festival_period.info_id = ti.id
        <where>
            <if test="scope != null">
                ti.scope = #{scope}
            </if>
            <if test="contentType != null">
                AND ti.content_type = #{contentType}
            </if>
            <if test="categoryId != null">
                AND ti.category_id = #{categoryId}
            </if>
        </where>
        ORDER BY ti.created_at DESC, ti.id DESC
    </select>

    <!--
      대표 행사기간. 진행중 → 예정 → 종료 우선으로 한 줄만 고른다.
      목록에 찍는 기간, 행사 상태 판정, 행사일순 정렬이 모두 이 한 줄을 본다.
      기간이 여러 건인 기존 데이터도 같은 규칙으로 한 줄만 대표가 된다.

      이 계산이 필요한 요청은 셋뿐이다 — 축제 목록(기간을 찍는다),
      행사 상태로 거르는 요청, 행사일순 정렬. 그 밖에는 붙이지 않는다.
      일반 여행정보는 ti.content_type = 'FESTIVAL' 이 늘 거짓이라 예전에도 결과가 NULL 이었다.
      (유형을 가리지 않는 호출이 생기면 축제가 섞일 수 있으므로 그때도 붙인다)
    -->
    <sql id="RepresentativePeriodJoin">
        LEFT JOIN info_periods representative_period
          ON representative_period.id = (
              SELECT period_candidate.id
              FROM info_periods period_candidate
              WHERE period_candidate.info_id = ti.id
                AND ti.content_type = 'FESTIVAL'
              ORDER BY
                  CASE
                      WHEN period_candidate.start_date &lt;= CURDATE()
                           AND period_candidate.end_date >= CURDATE() THEN 0
                      WHEN period_candidate.start_date > CURDATE() THEN 1
                      ELSE 2
                  END ASC,
                  CASE
                      WHEN period_candidate.start_date > CURDATE()
                          THEN period_candidate.start_date
                  END ASC,
                  CASE
                      WHEN period_candidate.end_date &lt; CURDATE()
                          THEN period_candidate.end_date
                  END DESC,
                  period_candidate.start_date ASC,
                  period_candidate.id ASC
              LIMIT 1
          )
    </sql>

    <!-- 행사 상태는 대표 행사기간과 오늘 날짜로만 정한다. 허용된 값만 서비스에서 넘어온다. -->
    <sql id="EventStatusFilter">
        <choose>
            <when test="eventStatus == 'ongoing'">
                AND representative_period.start_date &lt;= CURDATE()
                AND representative_period.end_date >= CURDATE()
            </when>
            <when test="eventStatus == 'upcoming'">
                AND representative_period.start_date > CURDATE()
            </when>
            <when test="eventStatus == 'ended'">
                AND representative_period.end_date &lt; CURDATE()
            </when>
        </choose>
    </sql>

    <sql id="PublicListFilters">
        <where>
            ic.is_visible = 1
            <if test="eventStatus != null">
                <include refid="EventStatusFilter"/>
            </if>
            <if test="scope != null">
                AND ti.scope = #{scope}
            </if>
            <if test="contentType != null">
                AND ti.content_type = #{contentType}
            </if>
            <if test="categoryIds != null and !categoryIds.isEmpty()">
                AND ti.category_id IN
                <foreach collection="categoryIds"
                         item="categoryId"
                         open="("
                         separator=","
                         close=")">
                    #{categoryId}
                </foreach>
            </if>
            <if test="keywordPattern != null and !keywordPattern.isEmpty()">
                AND (
                    ti.title LIKE CONCAT('%', #{keywordPattern}, '%') ESCAPE '!'
                    <if test="koreanPattern != null and !koreanPattern.isEmpty()">
                        OR REGEXP_LIKE(ti.title, #{koreanPattern})
                    </if>
                )
            </if>
        </where>
    </sql>

    <!--
      후보를 고르고 줄 세워 한 쪽만 남긴다.

      대표 행사기간은 이 요청이 실제로 쓸 때만 붙인다 — 축제 목록이거나, 행사 상태로 거르거나,
      행사일순으로 줄 세울 때다. 붙이지 않는 요청에서는 예전에도 늘 NULL 이었으므로
      빈 날짜 칸을 그대로 내보낸다.
    -->
    <sql id="PublicListPage">
        SELECT ti.id,
               ti.title,
               ti.scope,
               ti.content_type,
               ti.category_id,
               ic.name AS category_name,
               ti.views,
               ti.created_at,
        <choose>
            <when test="contentType == null or contentType.name() == 'FESTIVAL'
                        or eventStatus != null or sort == 'event'">
               representative_period.start_date,
               representative_period.end_date
            </when>
            <otherwise>
               CAST(NULL AS DATE) AS start_date,
               CAST(NULL AS DATE) AS end_date
            </otherwise>
        </choose>
        FROM travel_info ti
        JOIN info_categories ic ON ic.id = ti.category_id
        <if test="contentType == null or contentType.name() == 'FESTIVAL'
                  or eventStatus != null or sort == 'event'">
            <include refid="RepresentativePeriodJoin"/>
        </if>
        <include refid="PublicListFilters"/>
        <include refid="PublicListCandidateOrder"/>
        LIMIT #{limit}
        OFFSET #{offset}
    </sql>

    <!--
      줄 세우는 규칙.

      아래 PublicListPageOrder 와 반드시 같은 뜻이어야 한다. 여기가 어느 줄을 남길지 정하고,
      저기가 남은 줄을 같은 차례로 내보낸다. 둘이 어긋나면 쪽마다 차례가 흔들린다.
      (TravelInfoListSqlContractTest 가 두 식이 같은지 확인한다)
    -->
    <sql id="PublicListCandidateOrder">
        <choose>
            <when test="sort == 'views'">
                ORDER BY ti.views DESC, ti.created_at DESC, ti.id DESC
            </when>
            <!--
              행사일순. 오늘 갈 수 있는 행사 → 곧 시작할 행사 → 끝난 행사 차례로 본다.
              같은 묶음 안에서는 진행중은 먼저 끝나는 순, 예정은 먼저 시작하는 순,
              종료는 최근에 끝난 순이다.
            -->
            <when test="sort == 'event'">
                ORDER BY
                    CASE
                        WHEN representative_period.start_date &lt;= CURDATE()
                             AND representative_period.end_date >= CURDATE() THEN 0
                        WHEN representative_period.start_date > CURDATE() THEN 1
                        ELSE 2
                    END ASC,
                    CASE
                        WHEN representative_period.start_date &lt;= CURDATE()
                             AND representative_period.end_date >= CURDATE()
                            THEN representative_period.end_date
                    END ASC,
                    CASE
                        WHEN representative_period.start_date > CURDATE()
                            THEN representative_period.start_date
                    END ASC,
                    CASE
                        WHEN representative_period.end_date &lt; CURDATE()
                            THEN representative_period.end_date
                    END DESC,
                    ti.id DESC
            </when>
            <otherwise>
                ORDER BY ti.created_at DESC, ti.id DESC
            </otherwise>
        </choose>
    </sql>

    <!-- 남은 한 쪽을 같은 차례로 내보낸다. 파생 테이블이 담아 둔 순서에 기대지 않는다. -->
    <sql id="PublicListPageOrder">
        <choose>
            <when test="sort == 'views'">
                ORDER BY page.views DESC, page.created_at DESC, page.id DESC
            </when>
            <when test="sort == 'event'">
                ORDER BY
                    CASE
                        WHEN page.start_date &lt;= CURDATE()
                             AND page.end_date >= CURDATE() THEN 0
                        WHEN page.start_date > CURDATE() THEN 1
                        ELSE 2
                    END ASC,
                    CASE
                        WHEN page.start_date &lt;= CURDATE()
                             AND page.end_date >= CURDATE()
                            THEN page.end_date
                    END ASC,
                    CASE
                        WHEN page.start_date > CURDATE()
                            THEN page.start_date
                    END ASC,
                    CASE
                        WHEN page.end_date &lt; CURDATE()
                            THEN page.end_date
                    END DESC,
                    page.id DESC
            </when>
            <otherwise>
                ORDER BY page.created_at DESC, page.id DESC
            </otherwise>
        </choose>
    </sql>

    <!--
      목록 카드의 대표 이미지.

      예전에는 조건에 맞는 모든 줄마다 이 서브쿼리를 돌린 뒤 열두 줄만 남겼다.
      지금은 쪽을 자른 뒤라 남은 줄에 대해서만 돈다.
      고르는 규칙은 예전과 같다 — 축제는 썸네일로 지정한 것을 먼저, 그 다음 대표 이미지,
      일반 여행정보는 대표 이미지만. 같은 순위 안에서는 order_index, id 차례다.
      (유형이 섞여 들어올 수 있는 호출은 예전처럼 줄마다 유형을 보고 고른다)
    -->
    <sql id="PublicListThumbnail">
        (
            SELECT ii.image_url
            FROM info_images ii
            WHERE ii.info_id = page.id
        <choose>
            <when test="contentType != null and contentType.name() == 'FESTIVAL'">
              AND (ii.is_thumbnail = 1 OR ii.is_main = 1)
            ORDER BY CASE WHEN ii.is_thumbnail = 1 THEN 0 ELSE 1 END ASC,
                     ii.order_index ASC, ii.id ASC
            </when>
            <when test="contentType != null">
              AND ii.is_main = 1
            ORDER BY ii.order_index ASC, ii.id ASC
            </when>
            <otherwise>
              AND (
                  (page.content_type = 'FESTIVAL' AND (ii.is_thumbnail = 1 OR ii.is_main = 1))
                  OR (page.content_type != 'FESTIVAL' AND ii.is_main = 1)
              )
            ORDER BY CASE
                         WHEN page.content_type = 'FESTIVAL' AND ii.is_thumbnail = 1 THEN 0
                         ELSE 1
                     END ASC,
                     ii.order_index ASC, ii.id ASC
            </otherwise>
        </choose>
            LIMIT 1
        ) AS thumbnail_url
    </sql>

    <select id="findPublicList" resultMap="PublicListResultMap">
        SELECT page.id,
               page.title,
               page.scope,
               page.content_type,
               page.category_id,
               page.category_name,
               <include refid="PublicListThumbnail"/>,
               page.views,
               page.created_at,
               page.start_date,
               page.end_date
        FROM (
            <include refid="PublicListPage"/>
        ) page
        <include refid="PublicListPageOrder"/>
    </select>

    <!--
      개수 세기는 화면에 그릴 값을 하나도 만들지 않는다.
      대표 행사기간은 행사 상태로 거를 때만 필요하다 — 그때만 붙여야 쪽 수가 맞는다.
      축제 목록이라는 이유만으로 붙이지 않는다. 거르는 조건은 목록과 같은 조각을 쓴다.
      (info_categories 조인은 ic.is_visible = 1 을 보기 위한 것이라 남긴다.
       travel_info.category_id 가 NOT NULL 이고 ic.id 가 PK 라 줄 수를 바꾸지 않는다)
    -->
    <select id="countPublicList" resultType="long">
        SELECT COUNT(*)
        FROM travel_info ti
        JOIN info_categories ic ON ic.id = ti.category_id
        <if test="eventStatus != null">
            <include refid="RepresentativePeriodJoin"/>
        </if>
        <include refid="PublicListFilters"/>
    </select>

    <update id="incrementPublicViews">
        UPDATE travel_info ti
        JOIN info_categories ic
          ON ic.id = ti.category_id
         AND ic.is_visible = 1
        SET ti.views = ti.views + 1,
            ti.updated_at = ti.updated_at
        WHERE ti.id = #{id}
    </update>

    <select id="findPublicDetailById" parameterType="long" resultMap="PublicDetailResultMap">
        SELECT ti.id,
               ti.title,
               ti.scope,
               ti.content_type,
               ti.category_id,
               ic.name AS category_name,
               ti.content,
               ti.views,
               ti.created_at,
               ti.updated_at
        FROM travel_info ti
        JOIN info_categories ic ON ic.id = ti.category_id
        WHERE ti.id = #{id}
          AND ic.is_visible = 1
    </select>

    <select id="findPublicBookmarkTargetForUpdate" resultType="long">
        SELECT ti.id
        FROM travel_info ti
        JOIN info_categories ic ON ic.id = ti.category_id
        WHERE ti.id = #{id}
          AND ic.is_visible = 1
        FOR UPDATE
    </select>

    <select id="findById" parameterType="long" resultMap="TravelInfoResultMap">
        SELECT id, title, content, scope, content_type, created_at, updated_at,
               category_id, views, user_id
        FROM travel_info
        WHERE id = #{id}
    </select>

    <select id="findByIdForUpdate" parameterType="long" resultMap="TravelInfoResultMap">
        SELECT id, title, content, scope, content_type, created_at, updated_at,
               category_id, views, user_id
        FROM travel_info
        WHERE id = #{id}
        FOR UPDATE
    </select>

    <insert id="insertTravelInfo" parameterType="com.example.travlediary.model.TravelInfo"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO travel_info (
            title, content, scope, content_type, category_id, views, user_id, created_at, updated_at
        ) VALUES (
            #{title}, #{content}, #{scope}, #{contentType}, #{categoryId}, 0, #{userId}, NOW(), NOW()
        )
    </insert>

    <update id="updateTravelInfo" parameterType="com.example.travlediary.model.TravelInfo">
        UPDATE travel_info
        SET title = #{title},
            content = #{content},
            scope = #{scope},
            content_type = #{contentType},
            category_id = #{categoryId},
            updated_at = NOW()
        WHERE id = #{id}
    </update>

    <delete id="deleteTravelInfo" parameterType="long">
        DELETE FROM travel_info
        WHERE id = #{id}
    </delete>

    <select id="findPeriodsByInfoId" parameterType="long" resultMap="InfoPeriodResultMap">
        SELECT id, start_date, end_date, info_id
        FROM info_periods
        WHERE info_id = #{infoId}
        ORDER BY start_date ASC, end_date ASC, id ASC
    </select>

    <insert id="insertPeriod" parameterType="com.example.travlediary.model.InfoPeriod"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO info_periods (start_date, end_date, info_id)
        VALUES (#{startDate}, #{endDate}, #{infoId})
    </insert>

    <delete id="deletePeriodsByInfoId" parameterType="long">
        DELETE FROM info_periods
        WHERE info_id = #{infoId}
    </delete>

    <select id="findMainImageByInfoId" parameterType="long" resultMap="InfoImageResultMap">
        SELECT id, image_url, source_type, source_name, external_content_id, source_title,
               license_type, source_image_url, license_checked_at,
               is_main, is_thumbnail, order_index, created_at, info_id
        FROM info_images
        WHERE info_id = #{infoId}
          AND is_main = 1
        ORDER BY order_index ASC, id ASC
        LIMIT 1
    </select>

    <select id="findImagesByInfoId" parameterType="long" resultMap="InfoImageResultMap">
        SELECT id, image_url, source_type, source_name, external_content_id, source_title,
               license_type, source_image_url, license_checked_at,
               is_main, is_thumbnail, order_index, created_at, info_id
        FROM info_images
        WHERE info_id = #{infoId}
        ORDER BY order_index ASC, id ASC
    </select>

    <select id="findMainImageUrlsByInfoId" parameterType="long" resultType="string">
        SELECT image_url
        FROM info_images
        WHERE info_id = #{infoId}
          AND is_main = 1
        ORDER BY order_index ASC, id ASC
    </select>

    <insert id="insertInfoImage" parameterType="com.example.travlediary.model.InfoImage"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO info_images (
            image_url, source_type, source_name, external_content_id, source_title,
            license_type, source_image_url, license_checked_at,
            is_main, is_thumbnail, order_index, info_id, created_at
        )
        VALUES (
            #{imageUrl}, COALESCE(#{sourceType}, 'ADMIN_UPLOAD'), #{sourceName},
            #{externalContentId}, #{sourceTitle}, #{licenseType}, #{sourceImageUrl}, #{licenseCheckedAt},
            #{isMain}, COALESCE(#{isThumbnail}, 0), #{orderIndex}, #{infoId}, NOW()
        )
    </insert>

    <update id="clearThumbnailsByInfoId" parameterType="long">
        UPDATE info_images
        SET is_thumbnail = 0
        WHERE info_id = #{infoId}
          AND is_thumbnail = 1
    </update>

    <update id="setThumbnailByIdAndInfoId">
        UPDATE info_images
        SET is_thumbnail = 1
        WHERE id = #{imageId}
          AND info_id = #{infoId}
    </update>

    <delete id="deleteMainImagesByInfoId" parameterType="long">
        DELETE FROM info_images
        WHERE info_id = #{infoId}
          AND is_main = 1
    </delete>

    <!--
      여행정보 번역. GENERAL/FESTIVAL 공용이며 언어 한 줄이 단위다.
      언어 대체(요청 언어 -> ko -> 남은 언어)가 늘 같은 줄을 고르도록 정렬을 고정한다.
    -->
    <select id="findTranslationsByInfoId" parameterType="long"
            resultMap="TravelInfoTranslationResultMap">
        SELECT id, travel_info_id, language_code, title, content
        FROM travel_info_translations
        WHERE travel_info_id = #{infoId}
        ORDER BY language_code ASC, id ASC
    </select>

    <select id="findTranslationsByInfoIds" resultMap="TravelInfoTranslationResultMap">
        SELECT id, travel_info_id, language_code, title, content
        FROM travel_info_translations
        <choose>
            <when test="infoIds != null and !infoIds.isEmpty()">
                WHERE travel_info_id IN
                <foreach collection="infoIds" item="infoId"
                         open="(" separator="," close=")">
                    #{infoId}
                </foreach>
            </when>
            <otherwise>
                WHERE 1 = 0
            </otherwise>
        </choose>
        ORDER BY travel_info_id ASC, language_code ASC, id ASC
    </select>

    <insert id="insertTranslation"
            parameterType="com.example.travlediary.model.TravelInfoTranslation"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO travel_info_translations (travel_info_id, language_code, title, content)
        VALUES (#{travelInfoId}, #{languageCode}, #{title}, #{content})
    </insert>

    <update id="updateTranslation"
            parameterType="com.example.travlediary.model.TravelInfoTranslation">
        UPDATE travel_info_translations
        SET title = #{title},
            content = #{content}
        WHERE travel_info_id = #{travelInfoId}
          AND language_code = #{languageCode}
    </update>

    <delete id="deleteTranslation">
        DELETE FROM travel_info_translations
        WHERE travel_info_id = #{travelInfoId}
          AND language_code = #{languageCode}
    </delete>

</mapper>
