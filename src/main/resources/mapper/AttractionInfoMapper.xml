<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.travlediary.repository.info.AttractionInfoMapper">

    <insert id="insert" parameterType="com.example.travlediary.model.AttractionInfo">
        INSERT INTO attraction_info
        (destination_id, closed_days, opening_hours, admission_fee, parking_available, contact_number, homepage_url, guide)
        VALUES
            (#{destinationId}, #{closedDays}, #{openingHours}, #{admissionFee}, #{parkingAvailable}, #{contactNumber}, #{homepageUrl}, #{guide})
    </insert>

    <select id="findByDestinationId" resultType="com.example.travlediary.model.AttractionInfo">
        SELECT * FROM attraction_info WHERE destination_id = #{destinationId}
    </select>

    <select id="findTranslationsByDestinationId"
            resultType="com.example.travlediary.model.AttractionInfoTranslation">
        SELECT id,
               destination_id,
               language_code,
               closed_days,
               opening_hours,
               admission_fee,
               guide
        FROM attraction_info_translations
        WHERE destination_id = #{destinationId}
        ORDER BY language_code ASC, id ASC
    </select>

    <update id="update" parameterType="com.example.travlediary.model.AttractionInfo">
        UPDATE attraction_info
        SET
            closed_days = #{closedDays},
            opening_hours = #{openingHours},
            admission_fee = #{admissionFee},
            parking_available = #{parkingAvailable},
            contact_number = #{contactNumber},
            homepage_url = #{homepageUrl},
            guide = #{guide}
        WHERE destination_id = #{destinationId}
    </update>


</mapper>
