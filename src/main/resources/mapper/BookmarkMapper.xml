<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.travlediary.repository.bookmark.BookmarkMapper">

    <!-- 북마크 추가 (통합 구조) -->
    <insert id="insert" parameterType="com.example.travlediary.model.Bookmark">
        INSERT INTO bookmarks (user_id, target_type, target_id, created_at)
        VALUES (#{userId}, #{targetType}, #{targetId}, NOW())
            ON DUPLICATE KEY UPDATE created_at = NOW()
    </insert>

    <insert id="insertIgnore">
        INSERT IGNORE INTO bookmarks (user_id, target_type, target_id, created_at)
        VALUES (#{userId}, #{targetType}, #{targetId}, NOW())
    </insert>

    <!-- 북마크 삭제 (통합 구조) -->
    <delete id="delete">
        DELETE FROM bookmarks
        WHERE user_id = #{userId}
          AND target_type = #{targetType}
          AND target_id = #{targetId}
    </delete>

    <!-- 북마크 존재 여부 조회 (통합 구조) -->
    <select id="findByUserAndTarget" resultType="com.example.travlediary.model.Bookmark">
        SELECT id, user_id, target_type, target_id, created_at
        FROM bookmarks
        WHERE user_id = #{userId}
          AND target_type = #{targetType}
          AND target_id = #{targetId}
    </select>

    <!-- 특정 대상(여행지/게시글/코스 등)별 북마크 수 -->
    <select id="countByTarget" resultType="int">
        SELECT COUNT(*)
        FROM bookmarks
        WHERE target_type = #{targetType}
          AND target_id = #{targetId}
    </select>

    <!-- 유저가 북마크한 특정 타입의 대상 ID 목록 반환 -->
    <select id="findBookmarkedTargetIdsByUserId" resultType="long">
        SELECT target_id
        FROM bookmarks
        WHERE user_id = #{userId}
          AND target_type = #{targetType}
    </select>

    <select id="findBookmarkedTargetIds" resultType="long">
        SELECT target_id
        FROM bookmarks
        WHERE user_id = #{userId}
          AND target_type = #{targetType}
        <choose>
            <when test="targetIds != null and !targetIds.isEmpty()">
                AND target_id IN
                <foreach collection="targetIds"
                         item="targetId"
                         open="("
                         separator=","
                         close=")">
                    #{targetId}
                </foreach>
            </when>
            <otherwise>
                AND 1 = 0
            </otherwise>
        </choose>
    </select>

    <delete id="deleteByTarget">
        DELETE FROM bookmarks
        WHERE target_type = #{targetType}
          AND target_id = #{targetId}
    </delete>

    <delete id="deleteAllByUserId">
        DELETE FROM bookmarks
        WHERE user_id = #{userId}
    </delete>
</mapper>
