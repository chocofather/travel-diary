<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.travlediary.repository.translation.SharedTranslationCacheMapper">
    <resultMap id="cacheMap" type="com.example.travlediary.model.translation.SharedTranslationCache">
        <id property="id" column="id"/>
        <result property="sourceHash" column="source_hash" jdbcType="BINARY"/>
        <result property="sourceLanguage" column="source_language"/>
        <result property="targetLanguage" column="target_language"/>
        <result property="provider" column="provider"/>
        <result property="translationProfile" column="translation_profile"/>
        <result property="detectedSourceLanguage" column="detected_source_language"/>
        <result property="translatedText" column="translated_text"/>
        <result property="status" column="status"/>
        <result property="leaseToken" column="lease_token"/>
        <result property="leaseExpiresAt" column="lease_expires_at"/>
        <result property="retryAfter" column="retry_after"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <sql id="ExactKey">
        source_hash = #{sourceHash}
        AND source_language = #{sourceLanguage}
        AND target_language = #{targetLanguage}
        AND provider = #{provider}
        AND translation_profile = #{translationProfile}
    </sql>

    <select id="find" resultMap="cacheMap">
        SELECT id, source_hash, source_language, target_language, provider,
               translation_profile, detected_source_language, translated_text,
               status, lease_token, lease_expires_at, retry_after, created_at, updated_at
        FROM shared_translation_cache
        WHERE <include refid="ExactKey"/>
    </select>

    <insert id="insertProcessing"
            parameterType="com.example.travlediary.model.translation.SharedTranslationCache">
        INSERT IGNORE INTO shared_translation_cache (
            source_hash, source_language, target_language, provider, translation_profile,
            status, lease_token, lease_expires_at, created_at, updated_at
        ) VALUES (
            #{sourceHash}, #{sourceLanguage}, #{targetLanguage}, #{provider}, #{translationProfile},
            'PROCESSING', #{leaseToken}, #{leaseExpiresAt}, #{createdAt}, #{updatedAt}
        )
    </insert>

    <update id="tryClaim">
        UPDATE shared_translation_cache
        SET detected_source_language = NULL,
            translated_text = NULL,
            status = 'PROCESSING',
            lease_token = #{leaseToken},
            lease_expires_at = #{leaseExpiresAt},
            retry_after = NULL,
            updated_at = #{now}
        WHERE <include refid="ExactKey"/>
          AND (
              (status = 'PROCESSING' AND (lease_expires_at IS NULL OR lease_expires_at &lt;= #{now}))
              OR (status = 'FAILED' AND (retry_after IS NULL OR retry_after &lt;= #{now}))
              OR (status = 'READY' AND translated_text IS NULL)
          )
    </update>

    <update id="markReady">
        UPDATE shared_translation_cache
        SET detected_source_language = #{detectedSourceLanguage},
            translated_text = #{translatedText},
            status = 'READY',
            lease_token = NULL,
            lease_expires_at = NULL,
            retry_after = NULL,
            updated_at = #{now}
        WHERE <include refid="ExactKey"/>
          AND status = 'PROCESSING'
          AND lease_token = #{leaseToken}
    </update>

    <update id="markFailed">
        UPDATE shared_translation_cache
        SET detected_source_language = NULL,
            translated_text = NULL,
            status = 'FAILED',
            lease_token = NULL,
            lease_expires_at = NULL,
            retry_after = #{retryAfter},
            updated_at = #{now}
        WHERE <include refid="ExactKey"/>
          AND status = 'PROCESSING'
          AND lease_token = #{leaseToken}
    </update>

    <delete id="deleteProcessing">
        DELETE FROM shared_translation_cache
        WHERE <include refid="ExactKey"/>
          AND status = 'PROCESSING'
          AND lease_token = #{leaseToken}
    </delete>

    <insert id="insertReadyIfAbsent"
            parameterType="com.example.travlediary.model.translation.SharedTranslationCache">
        INSERT IGNORE INTO shared_translation_cache (
            source_hash, source_language, target_language, provider, translation_profile,
            detected_source_language, translated_text, status, created_at, updated_at
        ) VALUES (
            #{sourceHash}, #{sourceLanguage}, #{targetLanguage}, #{provider}, #{translationProfile},
            #{detectedSourceLanguage}, #{translatedText}, 'READY', #{createdAt}, #{updatedAt}
        )
    </insert>
</mapper>
